# Netlify config for email-signature
# Package directory: apps/email-signature (set in Netlify UI)
# Base directory: repository root (default)

[build]
  command = "pnpm nx build email-signature"
  publish = "apps/email-signature/dist"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
