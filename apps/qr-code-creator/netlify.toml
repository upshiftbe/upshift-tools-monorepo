# Netlify config for qr-code-creator
# Package directory: apps/qr-code-creator (set in Netlify UI)
# Base directory: repository root (default)

[build]
  command = "pnpm nx build qr-code-creator"
  publish = "apps/qr-code-creator/dist"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
