{
  "name": "upshift-tools-monorepo",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.14.2",
  "scripts": {
    "build": "nx run-many -t build --all",
    "dev": "nx run-many -t dev --all --parallel=10",
    "dev:overview": "nx dev tools-overview",
    "dev:qr-code-creator": "nx dev qr-code-creator",
    "dev:email-signature": "nx dev email-signature",
    "dev:schema-markup-generator": "nx dev schema-markup-generator",
    "dev:image-optimizer": "nx dev image-optimizer",
    "format": "nx format:write .",
    "lint": "nx lint"
  },
  "devDependencies": {
    "@nx/vite": "^22.5.3",
    "nx": "22.5.3"
  }
}
